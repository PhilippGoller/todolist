import { createContext } from "react";

/**
 * Context which represents if the app is currently displayed on a mobile device or another small screen.
 */
export const IsMobileContext = createContext(false);
